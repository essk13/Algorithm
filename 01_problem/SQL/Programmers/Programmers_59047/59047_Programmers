-- ANIMAL_INS 테이블의 ANIAML_ID와 NAME 컬럼 선택
SELECT ANIMAL_ID, NAME FROM ANIMAL_INS
-- 조건: NAME이 'el'을 포함(앞 뒤에 문자 존제 가능)하면서 ANIMAL_TYPE이 'Dog'인 것
WHERE NAME LIKE '%el%' AND ANIMAL_TYPE='Dog'
-- 이름 순으로 오름차순 정렬
ORDER BY NAME;